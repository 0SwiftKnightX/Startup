# Development Tools

This folder contains project-owned validation and development utilities.

- `verify/` is the shared project verification system.
- The verifier runner checks contracts and executes headless Godot runtime tests.

Tool output is evidence, not a substitute for Quest hardware acceptance. Keep generated build artifacts out of source control unless explicitly required.
