# Build Output

This folder is reserved for generated APKs and other local build output.

The Android Quest workflow writes `build/Startup-Quest3S-debug.apk` before uploading it as a GitHub Actions artifact. Generated binaries should normally remain untracked.
